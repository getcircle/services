machine:
    environment:
        DJANGO_SETTINGS_MODULE: services.settings.local
dependencies:
    override:
        - pip install --no-deps -r dev-requirements.txt
test:
    override:
        - python manage.py test profiles.tests.test_profiles.TestProfiles.test_get_direct_reports
