machine:
    environment:
        DJANGO_SETTINGS_MODULE: services.settings.local
dependencies:
    override:
        - pip install --no-deps -r requirements.txt
databases:
    pre:
        - python manage.py makemigrations
